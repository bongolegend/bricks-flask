# docs at https://cloud.google.com/appengine/docs/standard/python3/scheduling-jobs-with-cron-yaml

cron:
# - description: "run notifications"
#   url: /notify
#   schedule: every 30 minutes synchronized
# this means to start at 00:00 every day

